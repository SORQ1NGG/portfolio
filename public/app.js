const links = document.querySelectorAll('.link');const projectContainer = document.querySelector('.project__container');const filters = document.querySelectorAll('.filter__btn');const contactBtn = document.querySelector('.contact__btn');const firstName = document.querySelector('.first__name');const lastName = document.querySelector('.last__name');const email = document.querySelector('.email');const message = document.querySelector('.message');const toggleBtn = document.querySelector('.toggle__btn');const linkContainer = document.querySelector('.links__containers');links.forEach((link) => {    link.addEventListener('click', () => {        links.forEach(e => e.classList.remove('active'));        link.classList.add('active');    });});projects.forEach((project) => {    projectContainer.innerHTML += `        <div class="project__card" data-tags="#all, ${project.tags}">            <img src="img/${project.image}" alt="">            <div class="content">                <h1 class="project__name">${project.name}</h1>                <span class="tags">${project.tags}</span>            </div>        </div>    `;});filters.forEach((filterBtn) => {    filterBtn.addEventListener('click', () => {        let id = filterBtn.getAttribute('id');        let projectCards = document.querySelectorAll('.project__card');        projectCards.forEach(card => {            if(card.getAttribute('data-tags').includes(id)) {                card.classList.remove('hide');            }else {                card.classList.add('hide');            }        })        filters.forEach(btn => btn.classList.remove('active'));        filterBtn.classList.add('active');    })})contactBtn.addEventListener('click', () => {    if (firstName.value.length && lastName.value.length && email.value.length && message.value.length) {        fetch('/mail', {            method: 'post',            headers: new Headers({'Content-Type': 'application/json'}),            body: JSON.stringify({                firstName: firstName.value,                lastName: lastName.value,                email: email.value,                message: message.value,            })        })        .then(res => res.json())        .then(data => {            alert(data);        })    }});toggleBtn.addEventListener('click', () => {    toggleBtn.classList.toggle('active');    linkContainer.classList.toggle('show');});